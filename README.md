# tripletfinder
Identify spatial triplets of cells within a specified effective distance threshold. Pairwise distances are computed as the centroid-to-centroid distance minus the sum of the two cells' radii, where each radius is defined as the average of the minimum and maximum cell diameter columns.
